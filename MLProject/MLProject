name: credit-scoring-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir: {type: string, default: "creditscore_preprocessing"}
    command: "python modelling.py --data_dir {data_dir}"
